const initialState = {
  data: null,
  loading: false,
  error: null,
};

export function commentReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
